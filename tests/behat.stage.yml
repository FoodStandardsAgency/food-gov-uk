default:
  paths:
    features: features
    bootstrap: %behat.paths.features%/bootstrap
  extensions:
    Behat\MinkExtension\Extension:
    # include http auth data in base url when needed
      base_url: http://shield:down@stage.fsa.dev-1.siriusopensource.com/ 
    
imports: 
 - behat.yml