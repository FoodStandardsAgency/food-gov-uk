default:
  paths:
    features: features
    bootstrap: %behat.paths.features%/bootstrap
  context:
    class: Drupal\DrupalExtension\Context\Tag1Context
    parameters:
       drupal_users:
        editor:
          test/password
        admin:
          Tharg/password
        mail:
          test@codepositive.com/guWranujUnkot8/gmail.com/imap.gmail.com:993
       authentication:
          username: "shield"
          password: "down"
  extensions:
    Behat\MinkExtension\Extension:
    # include http auth data in base url when needed
      base_url: http://shield:down@dev.fsa.dev-1.siriusopensource.com/ 
      goutte: ~
      selenium2: ~
    Drupal\DrupalExtension\Extension:
        blackbox: ~
        region_map:
          Menu: "#block-superfish-1"  
          Log In: "#edit-actions"
          Page Title: "#branding"
          Drupal Menu: "#admin-menu-wrapper"
          Submit: "#edit-actions"
          Left Sidebar: ".l-sidebar-first"
          Right Sidebar: ".l-sidebar-second"
          Quote: ".field--name-field-fc-section-quote"
          News Block: "#block-views-news-block-1"
annotations:
  paths:
    features: features/annotations

closures:
  paths:
    features: features/closures
